# Office Booking — исходный код

Каркас проекта системы бронирования рабочих мест. Архитектура и описание — в `../README.md`.

- `frontend/` — клиентская часть (HTML/CSS/JS)
- `backend/` — серверная часть на PHP (REST API)
- `database/schema.sql` — схема БД MySQL
